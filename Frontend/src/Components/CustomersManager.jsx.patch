// PATCH TEMPORAL PARA CustomersManager.jsx
// Este archivo contiene las correcciones necesarias para el error de 'notes'

// Cambios necesarios en CustomersManager.jsx:

// 1. Línea ~481 (o donde esté el acceso a preferences.notes):
// CAMBIAR:
// {selectedCustomer.preferences.notes && (
// POR:
// {selectedCustomer.preferences?.notes && (

// 2. Línea ~484 (o donde esté el contenido de notes):
// CAMBIAR:
// {selectedCustomer.preferences.notes}
// POR:
// {selectedCustomer.preferences?.notes}

// 3. Línea ~453-454 (acceso a status):
// CAMBIAR:
// {getStatusBadge(selectedCustomer.status)}
// {selectedCustomer.status.toUpperCase()}
// POR:
// {getStatusBadge(selectedCustomer.status || 'active')}
// {(selectedCustomer.status || 'active').toUpperCase()}

// 4. Línea ~131 (en handleEditCustomer):
// AGREGAR al inicio de la función:
// if (!selectedCustomer?._id) {
//   console.error('No customer selected for editing');
//   return;
// }

// Estos cambios previenen errores cuando las propiedades son undefined
