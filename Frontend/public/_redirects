# Assets versionados con hash: el 404 honesto para archivos inexistentes lo
# maneja functions/[[slug]].js (Cloudflare _redirects NO soporta status 404,
# la regla anterior se ignoraba y dejaba el SPA fallback devolviendo text/html).

# SPA fallback para rutas del cliente (todo lo demás).
/*  /index.html  200
