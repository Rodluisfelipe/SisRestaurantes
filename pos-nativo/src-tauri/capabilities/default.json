{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Lo mínimo para operar: los comandos propios y los eventos que llevan la venta a la pantalla del cliente. Sin sistema de archivos, sin shell y sin red desde el webview: todo eso pasa por Rust, que es lo que permite tratar al webview como un entorno no confiable.",
  "windows": ["principal", "cliente"],
  "permissions": ["core:default"]
}
