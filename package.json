{
    "name": "sisrestaurantes",
    "version": "1.0.0",
    "scripts": {
      "start": "cd Backend && npm install && node server.js",
      "build": "cd Backend && npm install"
    },
    "engines": {
      "node": ">=14.0.0"
    }
  }